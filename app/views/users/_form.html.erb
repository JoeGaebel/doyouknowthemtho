<%= form_for(user) do |f| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :preferred_name %><br>
    <%= f.text_field :preferred_name %>
  </div>

  <div class="field">
    <%= f.label :mistaken_name %><br>
    <%= f.text_field :mistaken_name %>
  </div>

  <div class="field upload">
    <%= f.file_field :avatar, multiple: false %>
  </div>

  <div class="actions">
    <%= f.submit 'Create correction'%>
  </div>
<% end %>
